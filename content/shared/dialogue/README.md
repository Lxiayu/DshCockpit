# shared/dialogue（占位 · M4）

跨角色的基础语料库（`<topicId>.json`）在 **M4** 落地：条目含 key/条件/权重/时长，
红线是隐私校验（无 session/run id、无路径、无密钥样式字符串），运行时只消费本地库。

本目录当前仅为 `content/**` 内容工程的目录脚手架占位（见
`docs/notes/office-workbench-m0.md` §2）。M0 阶段没有任何语料文件；
工作台的发布校验只要求本 README 存在且非空。
