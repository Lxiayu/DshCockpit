# DshCockpit Virtual Office

## Agent Office 产品设计总纲 v0.1

> **Document Status:** Pre-Development / Product Design
> **Project:** DshCockpit
> **Feature:** Virtual Office / Agent Office
> **Current Phase:** 产品定义、交互设计、架构设计，暂不进入正式开发
> **Core Principle:** 先定义“产品是什么”，再决定“怎么实现”。

---

## 0. 实现词汇冻结（Task 0，2026-08-30）

> 本节由 Virtual Office implementation plan Task 0 / SPEC-00 冻结，作为总纲中约束第一版实现的唯一词汇来源；与下文历史章节冲突时，以本节和 `docs/specs/OFFICE-DESIGN-DISCUSSION.md` 的正式决策为准。契约测试：`test/office-doc-contract.test.js`。

1. **场景投影**：扁平 2D 正交办公室（Marvis 式扁平构图）；无消失点、无透视缩放、无动态摄像机，详见决策 32 / 决策 33。
2. **员工模型**：四名常驻员工 `orchestrator`（总控员）、`researcher`（研究员）、`coder`（编码员）、`reviewer`（审查员），外加最多一个动态 `collaborator` 席位；常驻员工永远 `presence=present`，不使用 offline。
3. **状态分层**：`presence / sync / runtime / activity / movement` 分层表达，其中 `movement: stationary | moving | arriving | leaving`；`activity=sleeping` 是本地行为，不代表离线、Session 结束或 Runtime 不可达。四层模型见决策 16；完整冻结枚举见 SPEC-00。
4. **Runtime 事实来源**：Harness 原生事件信封只有 `type/seq/time/data`（另有 `agent/status`、`subagent/start/end`、`turn/end` 等事件）；`eventId`、`sessionEpoch`、Office snapshot/resync 不是 Harness 原生字段，由 Office Adapter 派生并标记 `sequenceSource=adapter`。
5. **控制矩阵**：`cancel` / `interrupt` / `followup` / `steer` / `inject` 仅在兼容探针证明后暴露；pause/resume 不支持（第一版 UI 不暴露，也不伪造 paused 状态）；preempt 只是 Office 侧队列策略。
6. **存储与开关**：办公室设置唯一持久化来源是 `userData/office-state.v1.json`；feature flags 为 `officeRuntimeEnabled`、`officePlaygroundEnabled`。
   - **修订（2026-09-17，用户拍板 M4 直启动）**：`officeRuntimeEnabled` 默认改为 **`true`**（左栏入口一键进入办公室；`DSH_DESKTOP_OFFICE_RUNTIME=0` 可强制关闭）。`officePlaygroundEnabled` 保持 `false`（开发工具）。原"第一版默认均为 false"的约定自本条修订起失效。
7. **角色包来源**：第一版仅限内置角色、本地文件夹和本地 ZIP 导入；GitHub 导入与社区市场获取为后续阶段（post-MVP）。

---

# 1. 项目背景

DshCockpit 当前的核心定位，是将 DeepSeek Harness 从偏 CLI / Web 的 Agent 使用方式，包装为更加完整的桌面 Agent 管理环境。

当前 DshCockpit 已经具备一定的桌面化能力，例如：

* Desktop Shell
* Agent Runtime 管理
* Token / Context 信息
* 任务管理
* Runtime 状态
* 桌面级控制能力

但是，仅仅继续增加“功能”并不能解决产品传播问题。

对于开源项目而言，用户是否愿意：

* 记住项目
* 分享项目
* 制作内容
* 在社区讨论
* 为项目贡献代码
* 将项目推荐给别人

并不完全取决于功能数量。

因此，DshCockpit 需要从“开发者工具”进一步演化为：

> **一个可以让用户感知、观察和管理 AI Agent 的桌面环境。**

由此提出：

# DshCockpit Virtual Office

即：

> **让 AI Agent 真正“住进”桌面。**

---

# 2. 产品战略目标

Virtual Office 并不是单纯的“虚拟办公室 UI”。

它的核心目标是：

> **将原本抽象的 Agent Runtime 状态，转化为用户可以直接看到的“数字员工行为”。**

传统模式：

```text
User
 ↓
Agent
 ↓
Task
 ↓
Logs
 ↓
Result
```

Virtual Office 模式：

```text
User
 ↓
DshCockpit Office
 ↓
Agent Employee
 ↓
Visible Work / Behavior
 ↓
Task / Runtime / State
 ↓
Result
```

用户看到的不再只是：

> Agent 正在运行。

而是：

> “我的 Agent 正坐在办公室里干活。”

---

# 3. 设计灵感与参考原则

本项目可以研究腾讯 Marvis 的 Virtual Office 体验，但：

> **不复制 Marvis 的视觉设计、角色设计、UI 布局或具体资产。**

我们真正参考的是其产品思想：

1. 多 Agent 被赋予可识别的角色。
2. Agent 工作状态能够被直观感知。
3. Agent 没有任务时仍然拥有“日常状态”。
4. 抽象的 AI 工作流程被转换成用户熟悉的办公室场景。
5. AI 不只是一个聊天框，而像一个“生活在电脑中的团队”。

公开体验资料显示，Marvis 的虚拟办公室会让不同 Agent 处于工作、休息、打盹、喝咖啡等不同状态，从而使多 Agent 协作变得具象化。

DshCockpit 应吸收这种“可观察 Agent”理念，但建立自己的产品世界观。

---

# 4. 新产品定位

## 4.1 DshCockpit 不再只是：

> DeepSeek Harness Desktop GUI

而逐步升级为：

# AI Agent Desktop Environment

进一步可以形成：

```text
DshCockpit
    ↓
AI Agent Runtime Environment
    ↓
Virtual Office
    ↓
Agent Employees
    ↓
Character / Personality Ecosystem
```

---

# 5. 核心产品理念

Virtual Office 必须围绕以下四个关键词展开：

```text
Agent
Office
Personality
Runtime
```

而不是：

```text
图片
角色
动画
UI
```

前四者是产品核心。

后四者只是实现手段。

---

# 6. 核心概念模型

## 6.1 Agent = Virtual Employee

DshCockpit 中一个真正运行的 Agent，对应办公室中的一个虚拟员工。角色名称、头像、外观、性格均可以不同，但核心身份始终来自真实 Agent。第一版员工模型见“## 0. 实现词汇冻结”：四名常驻员工加一个动态 collaborator 席位。

### 6.1 历史角色示例（Superseded，2026-08-30）

> 早期草案曾用以下占位角色说明该概念；该示例已被 2026-08-30 实现词汇冻结取代，仅作历史背景，不作为实现输入。

```text
Agent A
Role: Frontend Developer

Agent B
Role: Researcher

Agent C
Role: QA

Agent D
Role: Documentation
```

---

# 7. Office = Agent 的可视化运行空间

Virtual Office 不是一个“装饰页面”。

它是：

> **Agent Runtime 的空间化可视化层。**

例如：

```text
Agent State
     ↓
Behavior System
     ↓
Character Animation
     ↓
Office Presentation
```

这意味着：

> 动画不是独立存在的，而应该由 Agent 状态驱动。具体状态分层与合法组合以“## 0. 实现词汇冻结”、决策 16 和 SPEC-00 的四层模型为准。

### 7.1 历史映射示例（Superseded，2026-08-30）

> 早期把 Runtime 事实直接写成 Working / Waiting / Completed 的单层映射示例，已被四层状态模型取代，仅作历史背景，不作为实现输入。

Agent 工作：

```text
Runtime = Working
↓
Agent 坐在电脑前
↓
开始工作动画
```

Agent 等待：

```text
Runtime = Waiting
↓
Agent 离开电脑
↓
等待 / 发呆
```

Agent 完成：

```text
Runtime = Completed
↓
Agent 做庆祝动作
```

---

# 8. Agent State System

第一版不把 `offline` 作为常驻员工状态。常驻员工始终 `presence=present`；同步异常使用独立的 `sync=healthy | stale | resyncing` 表达，连续无任务时进入睡眠行为而不是离线。

### 8.1 历史状态清单（Superseded，2026-08-30）

> 早期“第一阶段至少定义以下状态”的单层枚举已被四层状态模型（决策 16 / SPEC-00）取代，仅作历史背景，不作为实现输入。

```text
idle
working
thinking
waiting
blocked
completed
failed
```

### 8.3 历史扩展词（Superseded，2026-08-30）

> 早期列出的扩展状态词未被第一版采用；`paused` 对应的 pause/resume 控制当前不受 Harness 支持（见词汇冻结第 5 条）。仅作历史背景，不作为实现输入。

```text
queued
paused
cancelled
reconnecting
reviewing
interacting
```

### 8.2 状态字段要求

每个状态必须具备：

* 用户可理解的语义
* 对应的视觉状态
* 对应的行为
* 对应的动画
* 对应的 UI 信息

---

# 9. Agent Behavior System

Agent 状态应该映射到行为。

例如：

## Working

```text
坐在工位
打开电脑
敲击键盘
观察屏幕
```

## Thinking

```text
停下操作
思考
查看资料
短暂走动
```

## Waiting

```text
离开电脑
喝咖啡
等待
```

## Completed

```text
任务完成
起身
庆祝
```

## Failed

```text
出现异常
停止工作
表现出沮丧 / 困惑
```

## Sleeping / Sync anomaly

```text
Sleeping: 回到个人工位或休息点，趴桌睡觉
Sync anomaly: 保持在岗表现，仅显示同步诊断
```

---

# 10. “摸鱼”不是核心功能，而是产品人格

Virtual Office 可以存在：

* 打盹
* 喝咖啡
* 发呆
* 伸懒腰
* 聊天
* 看手机
* 休息

但是这些行为不能脱离 Agent 状态系统。

例如：

```text
Idle
+
空闲时间 > X
↓
可能触发：
喝咖啡
发呆
休息
```

默认 `X=5 分钟`，可在设置中调节。睡眠属于本地 activity，不代表 Session 结束、Runtime 不可达或员工离开办公室。

因此：

> “摸鱼”是 Agent 人格化表现，而不是简单的随机动画。

---

# 11. Office 世界观

第一版不建立过于复杂的游戏世界。

只设计一个：

# 小型开发者办公室

至少包含：

```text
工作区
休息区
咖啡区
公共空间
```

可能的家具：

* 办公桌
* 电脑
* 椅子
* 植物
* 文件柜
* 沙发
* 咖啡机
* 白板

但必须控制视觉复杂度。

原则：

> **办公室服务于 Agent，而不是 Agent 服务于场景。**

---

# 12. 第一版视觉目标

目标不是：

> “做一个非常华丽的 3D 办公室。”

目标是：

> **做一个统一、可信、有生命感的数字工作空间。**

必须避免：

* 不同来源素材拼接
* 角色比例不统一
* 场景透视不统一
* 光照不一致
* 角色脚底漂浮
* UI 和场景割裂
* 纯图片轮播产生的廉价感

---

# 13. 为什么不继续采用“整张 AI 图片 + 拼接”的方案

早期制作可以利用 AI 辅助生成概念图，但正式产品不能依赖：

```text
一张办公室图片
+
一张角色图片
+
另一张角色图片
+
简单拼接
```

因为这种方案很难保证：

* 空间一致性
* 透视一致性
* 光照一致性
* 比例一致性
* 动画一致性
* 状态切换
* 用户交互
* 可扩展性

正式方案必须建立：

```text
Scene
+
Objects
+
Character
+
Animation
+
State
+
UI
```

的系统化结构。

---

# 14. Character System

角色必须从办公室系统中独立出来。

一个角色至少包含：

```text
Character Identity
Visual Appearance
Animation Set
Personality
Behavior Rules
Metadata
License
```

---

# 15. Character Plugin Architecture

角色应该采用独立插件 / Asset Pack 体系。

建议结构：

```text
character-pack/

├── manifest.json
├── assets/
│   ├── avatar/
│   ├── sprites/
│   ├── animations/
│   └── expressions/
├── personality/
│   └── behavior.json
├── LICENSE
└── NOTICE
```

---

# 16. Manifest 基础信息

角色包至少需要声明：

```json
{
  "name": "...",
  "id": "...",
  "version": "...",
  "author": "...",
  "license": "...",
  "commercialUse": false,
  "assets": {},
  "personality": {}
}
```

未来可以增加：

```text
supportedStates
animationCapabilities
dependencies
preview
website
sourceRepository
attribution
```

---

# 17. 角色与 Agent 解耦

必须区分：

```text
Agent
```

与：

```text
Character
```

Agent 是实际运行的智能体。

Character 是其视觉人格载体。

因此：

```text
Agent #001
+
Character A
```

以后可以变成：

```text
Agent #001
+
Character B
```

甚至：

```text
同一个 Character
+
不同 Agent
```

---

# 18. 角色生命周期

角色系统需要支持：

```text
Install
Activate
Deactivate
Update
Remove
```

第一版角色包来源仅限内置角色、本地文件夹和本地 ZIP 导入（见决策 39 与“## 0. 实现词汇冻结”）。

用户可以：

* 安装角色
* 更换角色
* 关闭角色
* 恢复默认角色
* 从本地导入
* 后续阶段（post-MVP）从 GitHub 导入
* 后续阶段（post-MVP）从社区市场获取

---

# 19. 第三方角色生态战略

DshCockpit 可以支持第三方角色，但：

> **第三方角色永远不是 DshCockpit Core 的组成部分。**

正确关系：

```text
DshCockpit Core
       ↓
Character Plugin API
       ↓
Third-party Character
```

而不是：

```text
DshCockpit Core
       ↓
直接内置第三方 IP
```

---

# 20. DeepSeek Whale / 鲸鱼娘的阶段性定位

初期可以考虑支持：

> DeepSeek 鲸鱼娘 / 蓝鲸女仆等社区文化角色。

目的不是建立 DshCockpit 官方 IP。

而是：

> **利用已有社区认知降低产品认知门槛，获得冷启动传播势能。**

现有 DeepSeek Harness 社区已经存在鲸鱼娘独立皮肤、桌宠、Galgame 等项目，并普遍采用“代码与美术资产分开授权”的方式；一些项目明确将美术资源置于 CC BY-NC-SA 4.0 或保留原作者版权。

因此 DshCockpit 应采用：

# “生态支持，而不是品牌绑定”

---

# 21. 三层许可证体系

这是整个项目必须提前确定的基础架构。

---

## Layer 1 — Core Code

内容：

* Electron Shell
* Runtime Manager
* Agent 管理
* Office Engine
* Agent State System
* Character Plugin API
* 数据与逻辑代码

许可证：

# MIT License

目标：

保持完整开源。

允许：

* Fork
* 修改
* 二次开发
* 商业使用

---

## Layer 2 — Official Assets

内容：

* DshCockpit 官方角色
* 官方 Logo
* 官方插画
* 官方动画
* 官方场景
* 官方品牌资源

不自动继承 MIT。

必须拥有独立版权与授权说明。

未来可以支持：

* 商业授权
* 官方周边
* 品牌合作
* 商业插件
* 官方角色衍生品

---

## Layer 3 — Community / Third-party Assets

内容：

* 第三方角色
* 社区原创角色
* 第三方 IP
* DeepSeek Whale Maid 等社区角色

每个角色包必须遵循自己的 License。

例如：

```text
License:
CC BY-NC-SA 4.0

Commercial:
No

Author:
XXXX

Source:
XXXX
```

不能因为 DshCockpit Core 是 MIT，就自动把第三方角色变成 MIT。

---

# 22. 许可证隔离原则

必须形成：

```text
MIT
│
├── Core Code
│
└── Plugin API

Separate License
│
├── Official Assets
│
└── Community Assets
```

禁止形成：

```text
MIT
│
└── Everything
```

即：

> **代码开源 ≠ 所有角色、图片、品牌资源都开源。**

---

# 23. 第三方角色的安装模型

推荐：

```text
DshCockpit
    ↓
Character Center
    ↓
Available Characters
    ↓
DeepSeek Whale Maid
    ↓
Install
```

而不是：

```text
下载 DshCockpit
↓
默认自动携带第三方角色
```

第一阶段尤其建议：

> 第三方角色通过独立角色包安装。

---

# 24. 商业化兼容性

设计时必须提前考虑：

未来可能出现：

```text
DshCockpit Free
DshCockpit Pro
Official Character Packs
Community Marketplace
Paid Extensions
```

因此必须确保：

```text
第三方 Non-Commercial Character
```

不会意外进入：

```text
Commercial Distribution
```

未来如果产生商业发行版本：

必须能够：

* 排除非商业角色
* 替换默认角色
* 单独声明资产
* 独立管理授权

---

# 25. 产品传播战略

Virtual Office 的价值不是单纯让软件“可爱”。

真正的价值：

> **让用户拥有可以分享的 AI 故事。**

例如：

传统宣传：

> DshCockpit 支持 Token Monitoring。

新的宣传：

> “我的 AI 员工今天已经干了 13 个任务。”

传统：

> Agent Runtime 管理。

新的宣传：

> “我的 Agent 正在办公室里加班。”

传统：

> Task Completed。

新的宣传：

> “她终于把 Bug 修完了，然后跑去喝咖啡了。”

---

# 26. 可传播性设计

办公室系统应天然生成：

* 截图
* GIF
* 短视频
* Meme
* Agent 故事

例如：

```text
Today

Agents: 5
Tasks: 18
Completed: 14
Working: 2
Idle: 1

Tokens:
1.82M
```

同时画面中：

* 一个员工工作
* 一个员工喝咖啡
* 一个员工休息

这种“数据 + 情绪 + 角色”组合比单纯的数据面板更具有传播性。

---

# 27. Agent 人格系统

角色不仅仅拥有外观。

未来角色应拥有：

```text
Personality
Mood
Preference
Behavior
```

例如：

## Whale Maid

人格：

* 喜欢吃饭
* 工作时认真
* 空闲时摸鱼
* Token不足会抱怨

---

但以上行为应由：

```text
Personality
+
Agent State
+
Environment
```

共同决定。

---

# 28. 官方 IP 战略

鲸鱼娘等第三方角色只能作为：

# Growth Entry

未来必须逐步建立：

# DshCockpit Original Characters

目标：

最终形成：

```text
DshCockpit
   ↓
Dsh Office Universe
   ↓
Official Characters
   ↓
Community Characters
```

---

# 29. 品牌迁移路线

## Phase 1 — Community Growth

重点：

* 第三方社区角色
* 鲸鱼娘
* DeepSeek 社区文化
* Virtual Office

目标：

获得关注和用户。

---

## Phase 2 — Ecosystem

建立：

* Character SDK
* Character Marketplace
* Community Characters
* User-created Characters

目标：

让“角色”成为生态。

---

## Phase 3 — Original IP

推出：

* 官方角色
* 官方世界观
* 官方 Agent 人格
* 官方视觉资产

目标：

逐渐降低对第三方 IP 的依赖。

---

## Phase 4 — Platform

最终：

DshCockpit 不再因为某一个角色而存在。

而是：

> **任何 Agent 都可以进入 DshCockpit Office。**

---

# 30. MVP 定义

第一版不追求：

* 大地图
* 复杂动画
* 多楼层
* 3D 开放世界
* 社交系统
* 完整市场
* RPG 系统

MVP 只解决：

## ① 一个办公室

固定镜头。

---

## ② 多个 Agent 工位

每个 Agent 有自己的位置。

---

## ③ Agent State

常驻员工不显示 Offline；同步问题和 Runtime 事实分别在 `sync` 与 `runtime` 层展示。第一版状态词汇以“## 0. 实现词汇冻结（Task 0，2026-08-30）”的四层模型为准，不引入第二套枚举。

### ③ 历史状态枚举（Superseded，2026-08-30）

> 早期 MVP 单层状态枚举仅作历史背景，不作为实现输入。

```text
Idle
Working
Thinking
Waiting
Completed
Error
```

---

## ④ 基础行为

至少：

```text
工作
待机
休息
移动
完成
异常
```

---

## ⑤ Agent 信息面板

点击 Agent：

显示：

```text
Name
Role
Status
Current Task
Token Usage
Runtime
```

---

## ⑥ Character Plugin

至少支持：

```text
Load
Activate
Deactivate
Remove
```

---

# 31. 第一版必须避免的事情

在正式设计中禁止优先追求：

* “做得像 Marvis”
* “增加更多家具”
* “增加更多角色”
* “增加大量动画”
* “AI 随机生成办公室”
* “先把图片做得很好看”

正确顺序：

```text
产品模型
↓
交互模型
↓
状态模型
↓
视觉系统
↓
角色系统
↓
动画
↓
实现
```

---

# 32. 视觉设计原则

DshCockpit Office 应当建立独立视觉语言。

关键词：

```text
Developer
Warm
Cute
Professional
Desktop
Living
Stylized
Consistent
```

不要直接复制：

* Marvis 的办公室布局
* Marvis 的角色造型
* Marvis 的 UI
* Marvis 的配色
* Marvis 的具体镜头
* Marvis 的资产

应建立：

> DshCockpit 自己的“开发者办公室”。

---

# 33. 视觉一致性标准

任何进入最终产品的角色与场景必须满足：

```text
Perspective
Scale
Lighting
Rendering
Outline
Shadow
Color Language
Animation Style
```

统一。

特别禁止：

> “不同 AI 生成的图片直接拼起来”。

任何外部美术资源进入产品前，都必须经过统一处理。

---

# 34. 数据与视觉的关系

Office 不应该只读取 Agent 名称。

必须尽可能读取真实 Agent Runtime：

```text
Agent
Task
Status
Token
Context
Runtime
Error
Result
```

然后映射到：

```text
Character
Animation
Office State
UI State
```

形成：

# Runtime → Reality

---

# 35. 未来可扩展方向

未来可以考虑：

### Office Layout

用户自由摆放工位。

### Agent Relationship

Agent 可以：

* 交流
* 协作
* 讨论

### Mood

Agent 根据工作结果产生：

* 开心
* 疲惫
* 困惑
* 兴奋

### Statistics

办公室显示：

* 今日任务
* Token
* 完成数
* 工作时间
* 摸鱼时间

### Community

用户可以发布：

* Character
* Animation
* Office Theme
* Personality

---

# 36. 最终产品愿景

最终 DshCockpit 不应该让用户感觉：

> “我安装了一个 AI GUI。”

而应该让用户感觉：

> **“我的 AI 团队住在这里。”**

进一步：

```text
DshCockpit
=
AI Agent Desktop
+
Virtual Office
+
Agent Personality
+
Character Ecosystem
```

---

# 37. 当前设计阶段的最终结论

Virtual Office 不是一个独立小游戏。

它是：

> **DshCockpit Agent Runtime 的人格化可视化层。**

Character 不是核心逻辑。

Character 是：

> **Agent 的视觉人格。**

鲸鱼娘不是 DshCockpit 官方 IP。

鲸鱼娘应该作为：

> **Community Character / Growth Entry**

而 DshCockpit 官方 IP 应在后续逐步建立。

---

# 38. 下一阶段工作要求（历史阶段门禁，Superseded，2026-08-30）

> 本节写于规格未完成时；所需的规格已由 `docs/specs/OFFICE-DESIGN-DISCUSSION.md`、五份 Character Runtime 规格和 `docs/superpowers/` 下的 SPEC/plan 取代。仅作历史背景，不作为实现输入。

当前阶段：

**禁止直接进入大规模编码。**

下一阶段必须先完成以下设计文档：

```text
01-product-spec.md
02-agent-state-machine.md
03-office-interaction-spec.md
04-character-plugin-spec.md
05-character-manifest-spec.md
06-license-and-asset-policy.md
07-visual-design-system.md
08-mvp-development-plan.md
```

完成这些文档后，再进入开发。

---

# 39. 给后续 AI Agent 的最高优先级原则

请始终牢记：

> **不要为了“像 Marvis”而设计 DshCockpit。**

应该：

> 研究 Marvis 为什么有效，然后创造属于 DshCockpit 的 Agent Office。

不要把问题理解为：

> “如何做一个漂亮的虚拟办公室？”

而要理解为：

> **“如何把真实运行的 Agent 转化成一个具有生命感、可观察、可交互、可传播的数字员工？”**

最终目标不是：

> 一个漂亮的办公室。

而是：

# 一个真正“活着”的 AI Agent 工作空间。
