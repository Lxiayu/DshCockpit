# DshCockpit 路线图（ROADMAP）

> 更新：2026-08-20。基于对 `anywhere-labs/deepseek-harness-desktop` v2.0.x Release Notes 与 118 个 open issues 的逐条分析、`dsh-plugin` 星级榜单（9,883 个仓库）、以及生态最新数据的深度调研。
>
> 配套文档：[DESIGN.md](DESIGN.md)（架构）、[FEATURES.md](FEATURES.md)（已交付功能）、[README.md](README.md)（快速上手）。

---

## 0. 定位与原则

### 产品一句话

> 别的壳把 dsh web 装进窗口；DshCockpit 把 dsh 变成**常驻后台的 Agent 服务**，并提供完整桌面产品体验（更新、成本、后台任务、数据安全、手机遥控）。

### 三条铁律（所有新功能必须通过）

1. **守住"控制台"心智，不做 IDE** —— 不做内嵌终端/浏览器预览这类"工作台"功能，那是竞品的战场（也是他们闪退的重灾区），偏离 harness 定位。
2. **不重复造通信基础设施** —— 公网远程复用 Tailscale/Cloudflare 生态，不自研打洞/中继。
3. **护城河在工程深度，不在功能数量** —— 每个功能都要做到"别的薄壳抄不动"的深度（冒烟守卫、鉴权网关、成本归因、坏版本不激活）。

---

## 1. 已交付能力盘点（截至 v0.4.0）

| 能力 | 状态 | 说明 |
|---|---|---|
| 运行时/壳双层自动更新 + 回滚 | ✅ | 坏版本冒烟守卫不激活（最硬护城河） |
| Token 监控 + 成本中心 + 预算报警 | ✅ | 按工作区归因、峰谷分时、80%/100% 报警 |
| Quick Ask + 定时任务 + 通知 | ✅ | 全局热键 + headless 后台运行 |
| 会话全文检索 + 自动备份 | ✅ | Ctrl+K、append-only JSONL、三层保险 |
| 手机远程控制（局域网安全网关） | ✅ | 配对码/safeStorage/Origin 重写/防爆破 |
| 公网远程（Tailscale/Cloudflare） | ✅ | 默认关闭 + 显式授权 + 审计日志 |
| IM 渠道接入（飞书/企微/钉钉长连接） | ✅ | 事件推送 + 按钮审批 + IM 对话 |
| 官方级计费（余额 + 单轮成本） | ✅ | v4-flash/v4-pro 峰谷矩阵 + 缓存节省额 |
| 模型管理 + Ollama 本地接入 | ✅ | 6 预设 + 连接测试 + Key 加密 |
| 长会话管理（/compact + 记忆文件） | ✅ | 前后对比 + AGENTS.md 管理 |
| 技能（Skills）管理页 | ✅ | Claude Skill 兼容 + 预览防注入 |
| 插件市场（dsh-plugin 话题） | ✅ | 一键安装/卸载，重启生效 |
| 英文发布物料（Show HN + 新版 README.en） | ✅ | 2026-08 完成，见仓库根目录 LAUNCH-SHOW-HN-EN.md |
| 虚拟办公室（真事件驱动的走位/上工/审批收件箱） | ✅ | v0.4.0 起默认视图；右栏六块 IA + 危险模态审批 + 隐私边界 |
| 单元测试 | ✅ | **1393 项**（`npm test`，Windows/macOS/Linux 同一套） |
| macOS 签名与公证 | ⏸️ 暂缓 | 见 §3 决策记录 D-1 |
| Windows Authenticode 签名 | ⏸️ 暂缓 | 同上 |

---

## 2. 竞争情报（2026-08-20 调研结论）

### 2.1 大盘

| 指标 | 数值 |
|---|---|
| DeepSeek Harness 官方 | 174k★（持续爆发） |
| `dsh-plugin` 话题仓库 | 9,883 个（巨型项目蹭标签严重） |
| deepseek-harness-desktop | 16.4k★（仍在高速增长） |
| 最热品类 | 记忆/上下文基础设施（OpenViking 31k★/字节、EverOS 12.3k★、MemOS 10.9k★）——巨头赛道，勿碰 |

### 2.2 竞品强项（值得敬畏，勿低估）

Profile 管理、内置终端（私有 shim 不污染系统 PATH）、恢复助手、**macOS 签名公证已完成（v2.0.0）**、Mica 窗口效果、自建更新服务器 + 官网下载 CDN、dsh-market 集成、飞书文档站、30 贡献者。

### 2.3 竞品裂缝（issue 区实证，我们的机会）

| 问题模式 | 证据（issue #） | 根因 |
|---|---|---|
| 插件装不上/装了崩 | #351 #346 #341 #340 #348 | "万物皆插件"的代价：pnpm 边界 + Cordis slot 冲突 |
| 插件冲突导致启动即闪退、无恢复机制 | #325 | 插件跑在自己进程里，一个插件毒死整个桌面 |
| 打包遗漏导致无法启动 | #339 | vendor 整棵上游树的打包复杂度反噬 |
| **上游破坏性更新毁用户 profile、插件全丢** | v2.0.2 官方公告 | submodule pin 架构的原罪 |
| Windows 持续闪退/白屏 | #347 #326 #321 等 | — |
| 连"托盘重启应用"都在被求 | #335 | — |

> 战略含义：16.4k★ 背后有大量踩坑用户，是转化成本最低的受众。"从 DSH Desktop 迁移指南"是有真实需求的内容。

### 2.4 热门插件规律

火的项目分三类：①补官方缺的基础能力（codex-ui 套件、ModLens、dsh-context）②UI/情感化（dsh-web-ui 合集 5.2k★、colleague-skill 23.6k★）③找插件市场/榜单分发。共同点：解决"官方没有但人人需要"的缺口 + 名字直白 + 有 GIF。

> ⚠️ 预警：dsh-web-ui 已有插件级 "live token stats" 和 "mobile remote UI"。我们引以为豪的功能正在被插件层复刻——护城河必须往插件层做不了的深度走（预算报警、余额、工作区归因、缓存经济学）。

---

## 3. 优先级路线图（2026-08 重排）

### P0 —— 可靠性即产品（当前窗口主题）

> 未来两个月不是"加更多功能"，而是"把可靠性变成产品、把稳定性变成内容"：
> 签名解决敢不敢装（暂缓，见 D-1）→ 自检解决装不装得上 → 兼容快报解决跟不跟得上 → 成本深水区解决想不想晒。

#### 🚧 v0.4.0（进行中）：上游 0.1.5 远程 API 迁移
> 实测（2026-09-12，对运行中的 0.1.5-rc.2 探针）：0.1.5 重做了整个远程 API —— 所有 `/api/*` 与事件流升级**都需要浏览器会话 Cookie**（无 Cookie 一律 401）；`/api/events.host|mux` 与 `host/session-status` 事件被 `/api/remote.mux` 的 `$events` 流复用协议取代（首个 item 为 `{type:'ready'}`）；`/api/session.list`、`/api/respond` 已移除（审批/提问改走 `$events/result` 回执）；`commands/execute` 参数新增 `submittedAttachments`。壳完成以下适配并双平台验证后，才把内置运行时升到 0.1.5：
- [ ] Cookie 化：把 token→Cookie 交换抽成共享组件，接入事件流 / harness-rpc / 审批应答 / 手机网关
- [ ] 事件流迁到 `/api/remote.mux` + `$events`，重新定位"任务运行中/结束"事件（`turn/start|end` 等）
- [ ] 用 typert descriptor 重建会话列表 / 审批应答端点与参数
- [ ] 双平台 compat 冒烟 + 真机跑通：通知、IM 审批、/compact、按轮成本
- [ ] 内置运行时 `runtimeVersion` 升到 0.1.5-rc.2 并发布 0.4.0

#### R1 启动自检 + 一键修复 + 托盘重启
- **为什么**：竞品 issue 区最大的火灾就是"装不上/启动崩/没恢复"。我们有独家冒烟测试能力，把它产品化即可直接对打；托盘"重启应用"是他们用户在求的基本项。
- **做什么**：
  - 启动时跑环境自检（runtime 可执行/profile 完整性/目录可写/凭据存在），输出绿勾报告；
  - 失败项给一键修复（重建 profile 链接、清理损坏 runtime、重置端口）；
  - 托盘菜单加"重启运行时 / 重启应用"；
  - 自检结果落盘 `userData/diagnostics/boot-report.json`，崩溃诊断可直接引用。
- **验收标准**：
  - [ ] 干净环境/损坏 runtime/坏 profile 三种场景下自检均给出准确判定与可执行的修复动作；
  - [ ] 修复过程不触碰会话数据（只动 runtime 与链接层）；
  - [ ] 托盘重启运行时不丢窗口状态；
  - [ ] 新增单元测试 ≥ 8 项，全绿。

#### R2 上游兼容性快报管道
- **为什么**：竞品滞后于上游且用户已被破坏性更新烧过；"谁先宣布兼容"就是用户的决策瞬间。我们有独家双平台 CI 冒烟管道，边际成本极低。
- **做什么**：
  - 上游发新版 → CI 自动拉取并在 win/mac 双平台跑安装+冒烟+健康检查；
  - 结果自动生成兼容性报告（GitHub Discussion 模板 + README 动态徽章 `✅ rc.X verified`）；
  - 应用内"检查更新"页显示当前版本兼容状态。
- **验收标准**：
  - [ ] 新 rc 发布后 24h 内出报告；
  - [ ] 徽章随最新验证版本自动更新；
  - [ ] 冒烟失败自动标记并给出失败摘要。

### P1 —— 差异化钩子（吸引新用户的传播点）

#### R3 MCP 管理器 MVP
- **为什么**：MCP 已成"AI 的 USB-C"，40+ provider 用户都需要；壳级 MCP 管理器目前无人做，是空位。
- **做什么**：浏览/搜索 MCP registry → 一键添加到配置 → 连接测试 → 权限开关；后续补审计。
- **验收标准**：添加 ≥2 个真实 MCP Server 并完成一次工具调用测试；禁用/启用即时生效。

#### R4 缓存经济学看板
- **为什么**：V4-Pro 缓存命中价差约 30 倍，"前缀缓存省钱"是最强传播句式；数据层已就绪（token-stats 解析 usage），只差聚合呈现。
- **做什么**：按会话/天统计命中率与节省金额，可视化 + "本月缓存帮你省了 ¥X" 总卡片。
- **验收标准**：命中率/节省额与真实账单误差 <10%；可按会话下钻。

#### R5 Agent 周报卡片（Wrapped 式）
- **为什么**：把成本/token/任务数变成一张可分享图片卡，自带社交裂变；零竞品有。每周自动生成并可推送 IM 渠道。
- **做什么**：周聚合（复用 cost/token-stats 数据）→ 卡片渲染（离屏 BrowserWindow 截图）→ 保存/分享/推 IM。
- **验收标准**：卡片生成成功率高、双语、不含敏感信息（默认脱敏工作区路径）。

#### R6 通知中心
- **为什么**：竞品公告自己承认"不建议与其他通知类插件同用"——通知冲突是真痛点；壳级统一收口是正解。
- **做什么**：审批/完成/提问/预算告警统一进通知中心；免打扰时段、按类型开关、历史可查。
- **验收标准**：各事件源不再各自为政；免打扰生效；通知历史持久化可检索。

### P2 —— 护城河纵深

- **R7 插件健康度面板**：列出已装插件状态、已知冲突组合预警、坏插件隔离建议——定位"插件混乱中的稳定器"。
- **R8 多 Agent 并行看板**：并行任务/子 Agent 状态聚合监控，失败/阻塞告警（行业向 parallel agent teams 演进）。
- **R9 GitHub Pages 落地页**：复用 website/ 目录，Hero + 下载 + 对比表 + 兼容快报入口。

### ⏸️ 决策记录

- **D-1 签名与公证暂缓**（2026-08-20）：Apple Developer $99/年 + Windows 证书成本在仓库当前增长阶段投入产出不确定，暂缓投入。当前以"诚实的 xattr 指引 + HN 帖子坦诚披露"替代。**重启条件**（满足其一即重新评估）：GitHub star ≥ 500；或月度独立下载 ≥ 2,000；或出现企业/团队用户需求。届时优先 mac notarization（转化杀手），Windows Authenticode 次之。

### ❌ 明确不做清单

- ❌ 不做完整 IDE（内嵌终端/浏览器预览/多 Tab 工作区）——竞品战场，偏离控制台心智。
- ❌ 不碰记忆/RAG/上下文基础设施——OpenViking/MemOS/EverOS 等巨头赛道。
- ❌ 不自研打洞/中继——公网远程一律复用 Tailscale/Cloudflare。
- ❌ 不做模型绑定或闭源化——保持 BYOM。

---

## 4. 里程碑建议

- **M1（当前）**：R1 启动自检 → R2 兼容快报。配合 Show HN 发布节奏："可靠性"叙事成型。
- **M2（传播钩子）**：R4 缓存看板 → R5 周报卡片 → R3 MCP 管理器。
- **M3（纵深）**：R6 通知中心 → R7 插件健康度 → R9 落地页。
- **长期观望**：A2A/多智能体编排协议演进；插件经济模式；治理审计。

---

## 5. 参考来源

- [deepseek-harness-desktop](https://github.com/anywhere-labs/deepseek-harness-desktop)（v2.0.0–v2.0.2 Release Notes、118 open issues，2026-08-19/20 抓取）
- [dsh-plugin 话题星级榜](https://github.com/topics/dsh-plugin?o=desc&s=stars)（9,883 仓库，2026-08-20 抓取）
- [dsh-web-ui](https://github.com/zhu1090093659/dsh-web-ui)（5.2k★，UI 插件合集——功能复刻预警来源）
- [awesome-dsh-plugin](https://github.com/awesome-dsh-plugin/awesome-dsh-plugin)（10.6k★ 精选列表）
- [DeepSeek Harness 上线 3 天，4300 个第三方插件都在干什么（今日头条）](http://m.toutiao.com/group/7674810617052316214/)
